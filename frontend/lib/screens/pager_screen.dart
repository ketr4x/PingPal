import 'package:flutter/material.dart';

class PagerScreen extends StatelessWidget {
  const PagerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Pager')));
  }
}
